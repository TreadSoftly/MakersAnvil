# PASS-023 Report - Promoted Previous Workbench

## Goal

Make the user's previous working React application the authoritative Makers Anvil experience while retaining the current portable backend, guarded intake, contained execution, desktop shell, packaging, schemas, tests, and durable truth.

## Implemented

- Promoted the reviewed React source, industrial stylesheet, tool icons, rail, command search, source board, tool carousel, Work Flow/Plans/Dev views, proof inspector, contextual help, settings surfaces, responsive flow, and restrained motion into `frontend/`.
- Replaced the legacy machine-specific API client with a typed adapter over current `/api/state`, activity, authorized intake, and contained STL-preflight contracts.
- Kept external tool launch, native selected-file handoff, output opening, intake-folder opening, arbitrary route execution, folders, archives, and private path exposure visibly blocked.
- Routed picker, drag-and-drop, and clipboard file gestures through one exact one-file, short-lived, token-guarded authorization and app-owned quarantine copy.
- Changed browser, native desktop, and PyInstaller resource discovery to the compiled `frontend/dist` bundle.
- Retained all 20 prior React interaction tests, revised them to prove current endpoints and blockers, and added Python source gates for the promoted workbench.
- Updated source-manifest and every-line learning coverage for the React application and assets.

## Completion

- Full real application: `80.0000%`
- Windows local application: `84.0000%`
- Packaged release: `30.0000%`
- Clean-machine proof: `5.0000%`
- macOS/Linux application: `0.0000%`
- Browser-hosted application: `0.0000%`

These percentages are track-scoped. A working Windows-local application does not prove a signed installer, clean-machine behavior, other desktop platforms, or hosted-web operation.

## Verification

- `npm run build`: passed; Vite compiled the production React bundle.
- `npm run test -- --reporter=dot`: passed, 20 tests.
- Live loopback HTTP: compiled root and `/api/health` and `/api/state` returned current records.
- `python scripts/build_learning_guide.py`: passed for 161 selected sources and 36,030 explained lines at the verification point.
- `python scripts/check_explainability.py`: passed for all 214 tracked/pending product files at the verification point.
- `python scripts/verify_project.py`: all required-file, JSON, status, portability, reference, API, forbidden-text, and explainability groups passed.
- `python -m pytest -q`: passed, 163 tests.
- Native desktop smoke: passed with PASS-023, `80.0%`, current API build, guarded mutation scopes, and full route execution false.
- Windows one-file build: produced ignored `artifacts/windows/MakersAnvil.exe`; packaged `--smoke` passed with bundled PASS-023 React/core resources.
- Visual browser screenshot: not claimed because the in-app browser was unavailable in this session.

## Next Pass

PASS-024 restores promoted-workbench interaction parity through safe media previews and proof-gated desktop adapters, then begins splitting large frontend responsibilities without changing the accepted appearance or workflow.
